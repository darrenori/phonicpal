import './fonts';
import './tokens.css';
import './base.css';
import { applySettings } from './settings';

// Apply the reader's saved font, size, spacing, overlay and theme before first paint.
applySettings();
